extends ../layout/layout.pug

block content
  .row
    .col-md-12
      h1.page-header Create New Provision Template
  .row(ng-show="isSaving")
    .col-md-12
      include ../common/spinner.pug
  div
    .row
      .col-md-12
        include ./form.pug
    .row
      .col-md-12
        .buttons.text-right
          a.btn.btn-default(href="/provision-templates") Cancel
          button.btn.btn-primary(ng-click="createProvisionTemplate()" ng-disabled="provisionTemplateForm.$invalid || isSaving") Create
